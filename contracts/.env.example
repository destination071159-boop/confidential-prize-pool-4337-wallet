# Deployer key for Sepolia (0x-prefixed). Keep this secret; never commit the real .env.
PRIVATE_KEY=
# Sepolia RPC (Alchemy/Infura/public). Public default is rate-limited.
SEPOLIA_RPC_URL=https://rpc.sepolia.org
# For contract verification on Etherscan (optional).
ETHERSCAN_API_KEY=
